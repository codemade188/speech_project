# Speech Project 接口文档

## 1. `/`
- **方法**: `GET`
- **功能**: 返回项目简介。
- **响应示例**:
  ```json
  {
    "message": "这是flask开发的英语口语学习项目speech_project"
  }
  ```

---

## 2. `/api/auth`

### 2.1 `POST /api/auth/register`
- **功能**: 用户注册，接收用户名、邮箱和密码，生成验证码并发送邮件。
- **请求示例**:
  ```json
  {
    "username": "test_user",
    "email": "test@example.com",
    "password": "password123"
  }
  ```
- **响应示例**:
  ```json
  {
    "message": "Verification code sent to email."
  }
  ```

### 2.2 `POST /api/auth/verify`
- **功能**: 验证用户输入的验证码，完成注册。
- **请求示例**:
  ```json
  {
    "email": "test@example.com",
    "code": "123456"
  }
  ```
- **响应示例**:
  ```json
  {
    "message": "Registration successful."
  }
  ```

### 2.3 `POST /api/auth/login`
- **功能**: 用户登录，验证用户名和密码，成功后签发 JWT。
- **请求示例**:
  ```json
  {
    "username": "test_user",
    "password": "password123"
  }
  ```
- **响应示例**:
  ```json
  {
    "message": "Login successful.",
    "token": "<JWT_TOKEN>"
  }
  ```

### 2.4 `GET /api/auth/userinfo`
- **功能**: 获取当前登录用户的信息。
- **响应示例**:
  ```json
  {
    "username": "test_user",
    "email": "test@example.com"
  }
  ```

---

## 3. `/api/session`

### 3.1 `POST /api/session`
- **功能**: 创建新会话。
- **请求示例**:
  ```json
  {
    "session_name": "New Session"
  }
  ```
- **响应示例**:
  ```json
  {
    "message": "Session created successfully.",
    "session_id": "12345"
  }
  ```

### 3.2 `GET /api/session/user`
- **功能**: 列出当前用户的所有会话。
- **响应示例**:
  ```json
  [
    {
      "session_id": "12345",
      "session_name": "New Session",
      "created_at": "2025-12-20T12:00:00Z"
    }
  ]
  ```

### 3.3 `PUT /api/session/<session_id>`
- **功能**: 重命名指定会话。
- **请求示例**:
  ```json
  {
    "session_name": "Updated Session Name"
  }
  ```
- **响应示例**:
  ```json
  {
    "message": "Session renamed successfully."
  }
  ```

### 3.4 `PUT /api/session/<session_id>/activate`
- **功能**: 激活指定会话。
- **响应示例**:
  ```json
  {
    "message": "Session activated successfully."
  }
  ```

---

## 4. `/api/statistics`

### 4.1 `POST /api/statistics/practice_records`
- **功能**: 创建新的练习记录。
- **请求示例**:
  ```json
  {
    "session_id": "12345",
    "question_id": "67890",
    "audio_url": "http://example.com/audio.wav"
  }
  ```
- **响应示例**:
  ```json
  {
    "message": "Practice record created successfully."
  }
  ```

### 4.2 `GET /api/statistics/sessions/<session_id>/practice_records`
- **功能**: 获取指定会话下的所有练习记录。
- **响应示例**:
  ```json
  [
    {
      "record_id": "1",
      "question_id": "67890",
      "attempted_at": "2025-12-20T12:00:00Z",
      "summary": {
        "accuracy_score": 90,
        "fluency_score": 85,
        "integrity_score": 88,
        "total_score": 87
      },
      "audio_url": "http://example.com/audio.wav"
    }
  ]
  ```

### 4.3 `GET /api/statistics/practice_counts`
- **功能**: 获取用户的练习次数统计。
- **响应示例**:
  ```json
  {
    "total_practices": 100,
    "daily_practices": 5
  }
  ```

### 4.4 `GET /api/statistics/topic_distribution`
- **功能**: 获取用户练习的主题分布。
- **响应示例**:
  ```json
  [
    {
      "topic": "Business",
      "practice_count": 50
    },
    {
      "topic": "Travel",
      "practice_count": 30
    }
  ]
  ```

---

## 5. `/api/questions`

### 5.1 `GET /api/questions`
- **功能**: 获取所有问题。
- **响应示例**:
  ```json
  {
    "questions": [
      {
        "question_id": "1",
        "content": "What is your name?"
      },
      {
        "question_id": "2",
        "content": "Where are you from?"
      }
    ]
  }
  ```

### 5.2 `GET /api/questions/topics`
- **功能**: 获取所有主题及其问题数量。
- **响应示例**:
  ```json
  [
    {
      "topic_id": "1",
      "name": "Business",
      "question_count": 20
    },
    {
      "topic_id": "2",
      "name": "Travel",
      "question_count": 15
    }
  ]
  ```

---

## 6. `/api/evaluate`

### 6.1 `POST /api/evaluate/transcribe`
- **功能**: 使用 Whisper 模型对用户语音进行转写。
- **请求示例**:
  ```json
  {
    "audio_url": "http://example.com/audio.wav"
  }
  ```
- **响应示例**:
  ```json
  {
    "transcription": "Hello, how are you?"
  }
  ```

### 6.2 `POST /api/evaluate/ise`
- **功能**: 使用讯飞接口进行语音评估。
- **请求示例**:
  ```json
  {
    "audio_url": "http://example.com/audio.wav",
    "question_id": "12345"
  }
  ```
- **响应示例**:
  ```json
  {
    "accuracy_score": 90,
    "fluency_score": 85,
    "integrity_score": 88,
    "total_score": 87
  }
  ```
